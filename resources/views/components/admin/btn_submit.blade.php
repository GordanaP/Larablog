<button type="submit" name="{{ $name }}" value="{{ $value }}"
class="btn btn-sm btn-success btn-icon-split shadow-sm">
    <span class="icon text-white-50">
      <i class="fas fa-check"></i>
    </span>
    <span class="text">
        {{ $title }}
    </span>
</button>